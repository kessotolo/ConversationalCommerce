[alembic]
# path to migration scripts
script_location = backend/alembic

# Standard database URL - will be overridden by DATABASE_URL environment variable if set
# This enables direct use of alembic commands without custom logic
sqlalchemy.url = postgresql://postgres:postgres@localhost/conversational_commerce