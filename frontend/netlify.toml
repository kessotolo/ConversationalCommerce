[build]
  command = "cp .eslintrc-netlify.js .eslintrc.js && NEXT_DISABLE_ESLINT_DURING_BUILD=true npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

[[plugins]]
  package = "@netlify/plugin-nextjs"
