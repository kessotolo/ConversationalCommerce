/**
 * Core Models
 * 
 * These are the foundational types for the entire application.
 * All other modules should import types from here rather than
 * creating their own versions.
 */

// Export base types
export * from './base';
