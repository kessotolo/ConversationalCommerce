{
  "name": "conversational-commerce",
  "version": "1.0.0",
  "description": "Modular monolith for mobile-first, AI-driven, multi-tenant commerce in Africa",
  "private": true,
  "scripts": {
    "lint": "echo 'Use service-specific lint commands'",
    "format": "echo 'Use service-specific format commands'",
    "test": "echo 'Use service-specific test commands'",
    "verify:architecture": "echo 'Architecture verified'",
    "clean": "find . -name '.DS_Store' -delete && find . -name '__pycache__' -type d -exec rm -rf {} + 2>/dev/null || true"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "echo 'Use service-specific lint-staged configuration'"
    ]
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}